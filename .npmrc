# React Native + Expo compatibility:
# pnpm symlinks break Metro bundler and some native modules (react-native-webrtc autolinking).
# `hoisted` makes node_modules flat like npm, keeping pnpm's global store benefits.
node-linker=hoisted
